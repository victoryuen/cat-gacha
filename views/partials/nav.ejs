<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="/images/cat-icon.png" alt="Cat Gacha" width="40" height="40"
                class="d-inline-block align-text-top me-2">
            <div id="text-cat-gacha">
                Cat Gacha
            </div>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Links -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link <%= locals.active === 'home' ? 'active' : '' %>" href="/">Home</a>
                </li>
                <% if (locals.loggedIn) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= locals.active === 'collection' ? 'active' : '' %>"
                            href="/collection">Collection</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link <%= locals.active === 'gacha' ? 'active' : '' %>" href="/gacha">Play
                            Gacha</a>
                    </li>
                    <% } %>
            </ul>

            <!-- Auth Buttons -->
            <div class="d-flex">
                <% if (locals.loggedIn) { %>
                    <p id="logged-in-text"> Logged in as <%= locals.username %>
                    </p>
                    <form action="/logout" method="post">
                        <button type="submit" class="btn btn-outline-light me-2">Logout</button>
                    </form>
                    <% } else { %>
                        <a href="/login" class="btn btn-outline-light me-2">Login</a>
                        <a href="/signup" class="btn btn-light">Sign Up</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>